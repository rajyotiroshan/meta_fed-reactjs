/* import { useState } from "react"

export default function MealsList(){
    const todayMeals = [
        {name:"paratha" , calories: 100, id: 1},
        {name: "paneer", calories:150, id:2},
        {name: "milk", calories:100, id:3}
    ]

    const [meals, setMeals] = useState(todayMeals);

    return (

    )
} */
